from django.apps import AppConfig


class TransactionsConfig(AppConfig):
    """
    This class represents the Django application configuration
    for the 'transactions' app.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'transactions'
