from django.apps import AppConfig


class InvoicesConfig(AppConfig):
    """
    This class represents the Django application configuration
    for the 'invoices' app.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'invoices'
